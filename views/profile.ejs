<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="min-h-screen bg-zinc-900 text-white flex items-center justify-center p-6">
    <main class="w-full max-w-md">
      <section class="bg-zinc-800/70 backdrop-blur-sm rounded-2xl shadow-lg p-8">
        <div class="flex items-center gap-4 mb-6">
          <img src="<%= user.image %>" alt="avatar" class="w-20 h-20 rounded-full bg-zinc-700 object-cover" />
          <div>
            <h2 class="text-xl font-semibold"><%= user.name || user.username %></h2>
            <p class="text-sm text-zinc-400"><%= user.username %></p>
          </div>
        </div>

        <div class="space-y-3 text-sm text-zinc-300">
          <div>
            <span class="font-medium text-zinc-200">Email:</span>
            <span class="ml-2"><%= user.email %></span>
          </div>

          <div>
            <span class="font-medium text-zinc-200">Date of birth:</span>
            <span class="ml-2"><%= user.dateOfBirth ? (user.dateOfBirth.toISOString ? user.dateOfBirth.toISOString().slice(0,10) : user.dateOfBirth) : '-' %></span>
          </div>
        </div>

        <div class="mt-6 flex gap-3">
          <a href="/profile/edit" class="flex-1 py-2 text-center rounded-lg bg-gradient-to-r from-blue-600 to-indigo-600 hover:opacity-95">Edit profile</a>
          <a href="/auth/signout" class="flex-1 py-2 text-center rounded-lg bg-red-600 hover:opacity-95">Logout</a>
        </div>

        <p class="text-center text-xs text-zinc-500 mt-4">Simple profile view, I'll be adding more fields later.</p>
      </section>
    </main>
  </body>
</html>